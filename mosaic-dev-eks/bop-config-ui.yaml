apiVersion: v1
kind: ConfigMap
metadata:
    name: bop-config-ui-values
    namespace: application
data:
  values.yaml: |
     {
      replicaCount: 2, 
      fullnameOverride: "bop-config-ui"
      }

