apiVersion: v1
kind: ConfigMap
metadata:
    name: bop-config-api-values
    namespace: application
data:
  values.yaml: |
     {
      replicaCount: 2, 
      fullnameOverride: "bop-config-api"
      }

