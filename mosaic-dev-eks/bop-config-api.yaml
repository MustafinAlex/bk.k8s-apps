apiVersion: v1
kind: ConfigMap
metadata:
    name: bop-config-api-values
    namespace: application
data:
  values.yaml: |
     {
      replicaCount: 1, 
      fullnameOverride: "bop-config-api-2"
      }

